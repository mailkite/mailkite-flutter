# Docs: docs/architecture/client-side-oauth-libraries.md
name: mailkite_client_example
description: Minimal example app for the mailkite_client SDK.
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.0.0 <4.0.0"
  flutter: ">=3.10.0"

dependencies:
  flutter:
    sdk: flutter
  mailkite_client:
    path: ../

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
